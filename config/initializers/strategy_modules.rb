# frozen_string_literal: true

# Zeitwerk (config/initializers/zeitwerk.rb) collapses all module subdirs into
# the global autoload namespace — no explicit requires needed here anymore.
# Serializers are autoloaded on first reference.
