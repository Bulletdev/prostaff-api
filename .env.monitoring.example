GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=

# Alertmanager — email notifications
# Reuse the same SMTP credentials already configured in .env
SMTP_ADDRESS=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
ALERT_EMAIL_TO=
